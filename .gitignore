# Secrets
.env
.env.*

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# Data (users provide their own — see README.md § Data)
/data/**/preprocessed/
/data/**/raw/
/data/**/_filtered_source/

# Experiment artifacts (regenerated by reproduce.sh)
/models/
/output/

# Temporarily untracked — only covers the ablation/K-sweep, not Table 3's
# 7 baseline rows; holding back until the reproduction story is complete.
/reproduce.sh

# LaTeX build artifacts (if you build the paper locally)
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
*.fdb_latexmk
*.fls
